'use strict';

/**
 * survey-question-range router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::survey-question-range.survey-question-range');
