'use strict';

/**
 * survey-question-range service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::survey-question-range.survey-question-range');
