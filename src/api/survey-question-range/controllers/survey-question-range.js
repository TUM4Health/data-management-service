'use strict';

/**
 *  survey-question-range controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::survey-question-range.survey-question-range');
