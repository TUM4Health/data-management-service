'use strict';

/**
 * survey-question-select service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::survey-question-select.survey-question-select');
