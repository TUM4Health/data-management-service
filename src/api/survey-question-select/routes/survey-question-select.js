'use strict';

/**
 * survey-question-select router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::survey-question-select.survey-question-select');
