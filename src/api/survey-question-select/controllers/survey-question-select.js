'use strict';

/**
 *  survey-question-select controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::survey-question-select.survey-question-select');
