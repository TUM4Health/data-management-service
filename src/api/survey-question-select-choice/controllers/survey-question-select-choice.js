'use strict';

/**
 *  survey-question-select-choice controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::survey-question-select-choice.survey-question-select-choice');
