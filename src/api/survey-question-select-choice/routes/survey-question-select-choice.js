'use strict';

/**
 * survey-question-select-choice router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::survey-question-select-choice.survey-question-select-choice');
