'use strict';

/**
 * survey-question-select-choice service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::survey-question-select-choice.survey-question-select-choice');
