'use strict';

/**
 *  survey-question-freetext controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::survey-question-freetext.survey-question-freetext');
