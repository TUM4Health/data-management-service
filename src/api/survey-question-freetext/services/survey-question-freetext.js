'use strict';

/**
 * survey-question-freetext service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::survey-question-freetext.survey-question-freetext');
