'use strict';

/**
 *  survey-response-select controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::survey-response-select.survey-response-select');
