'use strict';

/**
 * survey-response-select service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::survey-response-select.survey-response-select');
